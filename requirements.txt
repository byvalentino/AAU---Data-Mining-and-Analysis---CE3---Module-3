# Pinned after a green build, never guessed.
#
# MLflow 3 replaced the sklearn default serialisation format with skops, which
# refuses to write a type it does not recognise -- and this module's pipeline
# carries StoredTransform, a class written here. Both log_model() calls
# therefore name cloudpickle explicitly. That parameter means the same thing in
# MLflow 2 and 3, so the code runs on either; the pins below are the pair the
# module is tested against.
#
# Moving to MLflow 3 is also what frees pyarrow. MLflow 2.17.2 required
# pyarrow<18, which is why this module used to pin 17.0.0 while the others
# pinned 18.1.0 -- one course, two versions of the same reader. MLflow 3.15.1
# asks for pyarrow>=4.0.0,<26, so all five modules now pin 18.1.0.
#
# Verified: Module 3 reaches four green checks on MLflow 3.15.1 and on 2.22.5.
# Not verified on this machine: the exact tuple below on Python 3.11, because
# pyarrow 18.1.0 has no wheel for macOS x86_64 there. Settle it once in the
# devcontainer:
#       bash setup.sh && make check          # expect data 0, labs 2 2 2 2
#       python3 apply.py && make check       # expect 0 0 0 0
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
scipy==1.14.1
scikit-learn==1.5.2
pytest==8.3.4
plotly==5.24.1
kaleido==0.2.1
mlflow==3.15.1
